angular.module( 'home', []).controller( 'HomeController', function( $scope, TFactory)
{
    /*
    $scope.$on( '$viewContentLoaded',
    function()
    {
        TFactory.one();
        TFactory.two();
        TFactory.three();
    });
    */
});
