angular.module( '3T', []).factory( 'TFactory', function( $window)
{
    return {
        /*
        one : function()
        {
            $window.alert( "one");        
        },
        two : function()
        {
            $window.alert( "two");        
        },
        three : function()
        {
            $window.alert( "three");        
        }
        */
    };
});
